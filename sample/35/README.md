# Install and run
## Dependencies
    `make run-dependencies`

## Run
    `make run`
For development only, you can use:

    `make run-dev`
      
# Testing  
## Unit tests  
    `make unit-test` 
## Integration tests
    `make integration-test`